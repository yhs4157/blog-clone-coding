let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_267‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_267‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_267‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_267‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_267‍.w("./api",[["default",["api"],function(v){api=v}]]);_267‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config(); 

 
 
 





const {PORT, MONGO_URI} = process.env; 

mongoose
.connect(MONGO_URI)
.then(() => {
    console.log("Connected to MongoDB");
})
.catch(e => {
    _267‍.g.console.error(e); 
});

// const api = require('./api');

const app = new Koa(); 
const router = new Router(); 

router.use('/api', api.routes());

app.use(bodyParser()); 
app.use(jwtMiddleware); 

app
.use(router.routes())
.use(router.allowedMethods);

const port = PORT || 4000; 

app.listen(port, () => {
    console.log(`Listening to port ${port}`);
});